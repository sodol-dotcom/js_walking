const greeting = "Hello";
const name = "World";
const result = greeting.concat(" ", name);

console.log(result); // "Hello World" 출럭

// 공백? -> const result = greeting.concat(" ", name);
// (수정 전)const result = greeting.concat(name);

